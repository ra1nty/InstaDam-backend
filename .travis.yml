dist: xenial
language: python
python: 3.7

services:
  - postgresql

install:
  - pip install -r requirements/test.txt

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres

script:
  - py.test --cov-report=xml --cov=instadam tests/

after_success:
  - codecov --token=de8254db-773b-4c7c-a847-05943628b8e0